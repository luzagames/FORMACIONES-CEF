/* Configuración del relay (ver README, sección "Relay").
   Después de desplegar el Worker de la carpeta relay/, pega aquí su dirección, por ejemplo:
     window.LU_CONFIG = { relay: 'https://alineaciones-relay.TU-CUENTA.workers.dev' };
   Con el valor vacío la app funciona como antes (mismo navegador, o servidor local). */
window.LU_CONFIG = { relay: '' };
